---
date: 2026-06-04
session: sess_2026-06-04_1700
actor_id: agent_claude
actor_type: ai-agent
sprint_week: "2026-06-02 / 2026-06-06"
tickets_worked: [SWLSVROS-6414]
---

# Daily — 2026-06-04 (Quarta-feira)

## Resumo

Melhoria de diagnósticos do CI: adicionadas 19 entradas `diag_for_test()` em `run.sh`
cobrindo todos os 25 testes não-passantes do run `results-2026-06-04_17-04-34`.

## Tarefas concluídas

### Diagnósticos — run.sh `diag_for_test()`

Adicionadas entradas para os 7 grupos de testes sem cobertura:

- **TC-HWPMC (12 testes)** — `ibs_hwpmc_alloc_test` (5 TCs), `ibs_hwpmc_caps_test` (2 TCs),
  `ibs_hwpmc_info_test` (3 TCs), `ibs_hwpmc_runtime_test` (2 TCs).
  Causa comum: IBS PMC backend (`PMC_CLASS_IBS`) não implementado nesta árvore
  libpmc/hwpmc. Pre-existente, independente do patch SWLSVROS-6414.

- **TC-PMCAPI (4 testes)** — `hwpmc_grouping_test`:
  `row_disposition_tracks_process_and_system_pmc`,
  `amd_pmu_core_events_allocate_concurrently`,
  `amd_pmu_mixed_core_events_allocate_concurrently`,
  `system_sampling_requires_logfile_before_start`.
  Causa: API `pmc_getpmcinfo()` disposition fields ou PMU JSON tables ausentes.
  Requer investigação separada.

- **TC-UMCUNIT (2 testes)** — `umcdf_unit_df_config_dispatch_test:umcdf_unit_dfdis_df1_df2_differ`
  e `umcdf_unit_capabilities_test:umcdf_unit_cap_df1_df2_differ`.
  Causa: `amd_umcdf_expected_df_config()` retorna valor idêntico para Zen3 e Zen4
  com evento 0x0107 — encode DF1/DF2 não diverge como esperado.

- **TC-PMCSTAT (1 teste)** — `pmcstat_ibs_errata_test:zen3_b0_fetch_icmiss_suppressed`.
  Causa: pmcstat não suprime `icmiss` para CPUIDs Zen 3 B0 — errata #1238 não aplicada
  ou faixa de CPUID errada no binário sob teste.

- **TC-NMISTR (1 teste, já tinha entrada)** — confirmado: `ibs_nmi_drain_under_load`
  já coberto desde sessão anterior.

## Commits do dia

Nenhum commit gerado nesta sessão — apenas edição em `run.sh`.

## Bloqueios / pendências

- TC-PMCAPI (4 testes): causa raiz desconhecida; precisa de run com dmesg/ktrace
  para diagnosticar falha do `pmc_getpmcinfo()` / PMU JSON.
- TC-UMCUNIT (2 testes): DF1 vs DF2 encoding mismatch precisa de revisão do código
  `amd_umcdf_expected_df_config()`.

## Sprint status

Semana 02/06–06/06: SWLSVROS-6414 entregue; diagnósticos completos para o run de 318P/25F/36S.
Zero regressões do patch confirmadas. Pendências são todas pre-existentes.
